services:
  - type: web
    name: pixocraft
    runtime: static
    buildCommand: npx vite build
    staticPublishPath: dist/public
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
