services:
  - type: web
    name: pixocraft
    runtime: static
    buildCommand: npm run build
    staticPublishPath: dist/public
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
