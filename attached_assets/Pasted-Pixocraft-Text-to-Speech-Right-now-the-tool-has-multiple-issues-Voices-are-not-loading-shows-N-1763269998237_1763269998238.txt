Pixocraft Text to Speech.

Right now the tool has multiple issues:

Voices are not loading (shows “No voices available”).

Browsers block speech synthesis inside embedded/iframe views.

Voice drop-down does not update on mobile.

Long paragraphs break the output box layout.

The “Speak” button works inconsistently and cuts audio early.

Speed, pitch, volume sliders do not update the live settings.

Need a fallback message:

“Open this tool in a new tab for full voice support.”

Your job is to FULLY FIX everything and rebuild the tool into a professional, stable, polished version.

✔ REQUIRED FEATURES

1. Voice Loader (must detect all browser voices)
Use this exact logic:

Wait for speechSynthesis.getVoices().

If empty → listen to speechSynthesis.onvoiceschanged.

Populate dropdown with voice name + language.

If still empty → show message “No voices available — open in new tab”.

2. Working Controls
Make speed, pitch, and volume fully working and connected:

Speed (0.5x to 2.0x)

Pitch (0 to 2)

Volume (0 to 1)

3. High-quality speak function

Cancel previous speech before starting new one

Prevent double-click issues

Never freeze

Smooth playback

Proper error handling

4. Proper responsive UI

Clean design

Fixed-height box for text input & output

Auto-scroll inside long text

Scrollbar visible but minimal

5. Embed View Detection
If inside iframe or embed →
Show a warning box:

“Voice output may not work inside embedded views. Click the button below.”

Button → Open in New Tab

6. Offline-only (NO external API)
Must use Browser SpeechSynthesis API only
No server, no cloud, no external JS libraries.

✔ FIX ALL KNOWN BUGS

Fix these issues:

"No voices available"

Voices not appearing on first load

Dropdown empty on mobile

Speech cutting early

Controls not updating voice

Long text breaking layout

Scrolling issues

iframe playback restriction warnings

Error “speechSynthesis.speak() failed”

✔ ADD EXTRA FEATURES

Add these enhancements:

Play / Pause / Resume / Stop buttons

Character counter (live)

Save last used voice settings (localStorage)

Button states (disabled when speaking)

Modern polished UI in HTML+CSS only

Dark/Light mode auto support

✔ FINAL OUTPUT FORMAT

Produce:

index.html

style.css

script.js

Everything must be clean, well-commented, organized, and production-ready for a static website.

✔ IMPORTANT RULES

No backend

No external API

No frameworks

100% static

Must work offline

Must support Chrome, Edge, Safari, Firefox

Must support Android + iOS

Must gracefully handle missing voices

🚀 GOAL

Create the best static Text-to-Speech tool in India for Pixocraft Tools.

Make it:

Fast

Smooth

Bug-free

Mobile-friendly

Professional-quality

Pixel-perfect