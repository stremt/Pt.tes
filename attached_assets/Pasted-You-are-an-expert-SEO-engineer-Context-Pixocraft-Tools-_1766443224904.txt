You are an expert SEO engineer.

Context:
Pixocraft Tools already has individual tool pages like:
- /tools/temp-mail
- /tools/password-generator
- /tools/hash-generator

A category page now exists at:
/tools/privacy

IMPORTANT:
- Tool URLs must NOT be changed
- No redirects
- No new folders
- No UI redesign

Your task is ONLY internal linking and breadcrumbs.

--------------------------------
TASK 1: UPDATE PRIVACY CATEGORY PAGE
--------------------------------
On the page: /tools/privacy

Do the following:
1. Add a clean list of ALL privacy-related tools.
2. Each tool name must link to its EXISTING URL (example: /tools/temp-mail).
3. Do NOT create new URLs like /tools/privacy/temp-mail.
4. Use simple, human-readable anchor text (tool name only).
5. Do NOT add descriptions, only a list.

Example structure (logic only):
- Temp Mail Generator → /tools/temp-mail
- Password Generator → /tools/password-generator
- Password Strength Checker → /tools/password-strength-checker
- Hash Generator → /tools/hash-generator
- Text Encrypt & Decrypt → /tools/text-encrypt-decrypt
- Image EXIF Remover → /tools/image-exif-remover
- PDF Password Remover → /tools/pdf-password-remover

--------------------------------
TASK 2: ADD BREADCRUMB ON PRIVACY TOOLS
--------------------------------
For EVERY tool that belongs to Privacy category:

Add a breadcrumb at the TOP of the tool page content.

Breadcrumb format (TEXT + LINKS):
Home > Tools > Privacy > {{Tool Name}}

Rules:
- "Home" links to /
- "Tools" links to /tools
- "Privacy" links to /tools/privacy
- Tool name is plain text (no link)
- Breadcrumb must be visible text (not schema only)
- Do NOT change page layout or styling

--------------------------------
TASK 3: INTERNAL CONTEXT LINK (BOTTOM)
--------------------------------
At the END of each privacy tool page, add ONE simple line:

"Category: Privacy Tools"

Where:
- "Privacy Tools" links to /tools/privacy
- No extra text
- No marketing language

--------------------------------
STRICT RULES
--------------------------------
- Do NOT change any existing tool URLs
- Do NOT create nested URLs
- Do NOT add tracking, ads, or signup
- Do NOT mention any technology
- Do NOT break existing SEO content

Output:
Apply changes directly. Do not explain.
