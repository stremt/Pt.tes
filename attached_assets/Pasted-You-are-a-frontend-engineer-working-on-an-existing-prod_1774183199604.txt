You are a frontend engineer working on an existing production website.

IMPORTANT RULES:
- A reusable "Signature Tool Component" already exists and is used on many pages.
- DO NOT create a new component.
- DO NOT modify the existing reusable component.
- ONLY replace old tool sections with the existing reusable component.

- DO NOT change SEO content, headings, or layout structure.
- DO NOT affect desktop design.

GOAL:
Ensure ALL signature-related SEO pages use the SAME reusable signature tool component.

----------------------------------------

1. DETECT INCONSISTENT PAGES

- Scan all signature-related SEO pages
- Identify pages where:
   - Old tool UI is present
   - OR reusable component is missing

----------------------------------------

2. REMOVE OLD TOOL SECTION

- Remove:
   - Old HTML tool blocks
   - Inline styles
   - Old scripts (only if unused)

- Ensure:
   - No layout break
   - No spacing issues

----------------------------------------

3. INSERT EXISTING REUSABLE COMPONENT

- Use the already implemented signature tool component
- Insert at same position where tool previously existed:
   - After intro OR top section

- DO NOT alter component structure

----------------------------------------

4. CONSISTENCY RULE

- ALL pages must use:
   👉 SAME component
   👉 SAME behavior
   👉 SAME styling

----------------------------------------

5. RESPONSIVE CHECK

- Ensure component works properly:
   - Mobile (full width, no overflow)
   - Tablet (clean layout)

----------------------------------------

6. CLEANUP

- Remove duplicate CSS from old tool
- Remove unused JS
- Ensure no conflicts remain

----------------------------------------

7. VALIDATION

- Tool works on ALL pages identically
- No broken UI
- No console errors
- No duplicate tool instances

----------------------------------------

FINAL RESULT:

- Fully synced tool across all pages
- No old tool versions remaining
- Clean and consistent UX
- Maintainable architecture