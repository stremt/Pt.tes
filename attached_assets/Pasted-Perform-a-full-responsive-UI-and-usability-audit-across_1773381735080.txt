Perform a full responsive UI and usability audit across ALL pages of the site, including:

• All SEO long-tail pages
• All main tool pages
• All category pages

Goals:

1. **Responsive Design (Desktop, Tablet, Mobile)**
   Ensure every page renders perfectly on:

* Desktop (≥1200px)
* Tablet (768px–1199px)
* Mobile (<768px)

All sections must resize properly with no layout breaking.

Requirements:

* Use responsive grids and flexible layouts.
* Avoid fixed widths on containers or cards.
* Ensure text, buttons, and cards scale correctly.
* Maintain consistent spacing and alignment across breakpoints.

Recommended CSS pattern:

```
grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
max-width: 1200px;
margin: 0 auto;
padding: 0 16px;
```

2. **Remove Horizontal Scrollbars**
   Many SEO pages currently create horizontal scroll.

Fix by:

* Removing containers that cause overflow
* Avoiding `width: 100vw`
* Avoiding `flex-wrap: nowrap`
* Avoiding fixed-width cards

Apply globally:

```
html, body {
overflow-x: hidden;
}
```

Ensure **NO page produces horizontal scrolling** on any device.

3. **Section Responsiveness**

Every section must adapt cleanly:

• hero sections
• tool interface sections
• content blocks
• FAQs
• related tools
• use-case cards

Cards must wrap automatically on smaller screens.

Example responsive rule:

```
display: grid;
grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
gap: 20px;
```

4. **SEO Long-Tail Pages: Hide “Create QR Code” Button**

On SEO pages, the top section contains a **“Create QR Code” button** that links to the main tool.

Action required:

• Hide this button on all SEO pages
• Do NOT remove it from the codebase — only hide it in SEO page templates
• Main tool pages must still keep their functional UI buttons

This avoids confusing UX while keeping SEO content intact.

5. **Preserve SEO Structure**

IMPORTANT — do not break any SEO elements:

Do NOT modify or remove:
• meta titles
• meta descriptions
• canonical tags
• structured data (FAQ, HowTo, etc.)
• internal links
• page URLs
• headings hierarchy (H1, H2, H3)

All layout changes must be **purely UI/UX and CSS related**.

6. **Improve Content Container Width**

Ensure long-form SEO content remains readable:

```
max-width: 900px;
line-height: 1.7;
```

Center content blocks for readability on large screens.

7. **Card Components Must Be Fully Responsive**

All cards (tools, use cases, related pages) must:

• resize naturally
• wrap correctly
• never overflow their container
• maintain consistent padding

8. **Mobile UX Improvements**

Ensure:

• buttons are at least 44px height
• tap areas are large enough
• headings wrap properly
• images scale within containers

9. **Final Verification**

After applying fixes:

• Test on Desktop
• Test on Tablet
• Test on Mobile

Confirm:

✔ No horizontal scroll anywhere
✔ Layout remains clean
✔ All sections responsive
✔ SEO markup untouched
✔ Navigation and internal links working

Goal:

Create a **fully responsive, production-quality layout** with clean UI across the entire website while preserving SEO performance and internal linking.
